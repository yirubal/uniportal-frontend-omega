# Development environment
# Railway backend for integration against the finished API.
# Leave blank to use the built-in dev mocks so the UI works without a server.
VITE_API_BASE_URL=https://web-production-312b.up.railway.app
VITE_FORCE_DEV_MOCKS=true
